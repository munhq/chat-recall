/*
 * Removed from history.
 *
 * This file held the transactional email copy while this repository was
 * public: subject lines, bodies, the send schedule and the reasoning behind
 * each message. The renderer now lives in @munhq/mailkit and the words in a
 * pack the deployment mounts, so no version of this file needs to carry them.
 *
 * The current version on main is unchanged. Only past revisions were replaced.
 */
