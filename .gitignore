# Dependencies
node_modules/

# Build output
dist/
web/client/dist/
web/server/dist/
*.js.map

# Environment (contains API keys)
.env

# LanceDB / SQLite index data
*.lance/
*.db
chat-recall-index/
chat-recall-cache.db

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Playwright
test-results/
playwright-report/
.playwright-mcp/

# Local screenshots / scratch (keep curated images explicitly tracked)
audit-*.png
settings-*.png
*-before.png
*-after.png
*-final*.png

# Codeindex per-server cache
.codeindex.json

# Coverage reports
coverage/

# Backup / patch artifacts
*.orig
*.rej

# Misc
*.tgz
*.log

# scratch screenshots from manual UI checks — never track root-level PNGs
/*.png
.tmp-*.png
/*.jpg
/*.jpeg
/*.gif

# Loose exported session transcripts / scratch dumps — never publish
session_*.txt

# Operator-only runbook (prod infra access) — never publish
CLAUDE.local.md

# Operator-only deploy script (private cluster blueprint) — never publish
scripts/deploy-local.sh

# design exports (kept locally, not part of the repo)
Chat Recall Design System.zip
chat-recall-design-system/

# TS project-reference build cache
*.tsbuildinfo
docker-compose.local.yml

# Generated per-repo task files (masked previews + leak/finding locations — not for VCS)
SECURITY_TASKS.md
CODE_TASKS.md
