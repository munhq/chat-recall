# docker-compose.override.yml — local development overrides.
# Automatically loaded by `docker compose`. Everything here is commented out;
# uncomment what you need.
#
# ── Bind the dashboard to localhost only (recommended with AUTH_PROVIDER=none) ──
#
# services:
#   server:
#     ports: !override
#       - "127.0.0.1:8080:5000"
